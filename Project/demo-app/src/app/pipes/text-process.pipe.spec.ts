import { TextProcessPipe } from './text-process.pipe';

describe('TextProcessPipe', () => {
  it('create an instance', () => {
    const pipe = new TextProcessPipe();
    expect(pipe).toBeTruthy();
  });
});
